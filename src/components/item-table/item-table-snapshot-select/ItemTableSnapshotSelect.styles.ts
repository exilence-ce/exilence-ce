import makeStyles from '@mui/styles/makeStyles';

const useStyles = makeStyles(() => ({
  selectionGroup: {
    height: 36,
  },
  MenuPaper: {
    maxHeight: 36 * 5 + 8,
  },
}));

export default useStyles;
